//This program will generate a number between 0 and 100. The user will guess a number.

#include <iostream>
#include <stdlib.h>
#include <time.h>

int main()
{
  srand(time(NULL));
  bool play = true;
  
  while(play == true)
    {
      int number = rand() % 1
    }
}
